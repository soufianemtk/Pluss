package facade;

import java.util.Collection;

import javax.servlet.http.HttpServletRequest;

import beans.Post;
import beans.TwitterAccount;

public class PublierImplementation implements Publier {
	
	private Collection<TwitterAccount> myAccounts;

	@Override
	public void diffuser(Post postToPublish, HttpServletRequest request) {
		/**
		this.recuperChamps(request);
		
		if(){
			//si twitter dans myAccounts On ouvre la connexion Twitter
			//recup les token et on envoie le post
		}
		if(){
			//si facebook dans myAccounts On ouvre la connexion Facebook
			////recup les token et on envoie le post
		}
		**/
	}

	@Override
	public void recuperChamps(HttpServletRequest request) {
		/**
		String valeur = request.getParameter( nomChamp );
		**/
	}
}
